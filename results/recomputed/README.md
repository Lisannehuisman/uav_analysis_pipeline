# Recomputed Outputs

<<<<<<< HEAD
Scripts in `src/` write new rerun outputs here by default. The published thesis figures, tables, and reports are kept in the sibling `figures`, `tables`, and `reports` folders.
=======
Scripts in `src/` write new rerun outputs here by default. The thesis figures, tables, and reports are kept in the sibling `figures`, `tables`, and `reports` folders.
>>>>>>> update-thesis-repo
